const styles = theme => ({
  root: {
    flexGrow: 1,
  },
  paper: {
    width: '31vw',
    padding: theme.spacing.unit * 2,
    color: theme.palette.text.secondary,
  },
});

export default styles;
