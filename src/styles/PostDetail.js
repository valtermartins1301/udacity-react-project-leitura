const styles = theme => ({
  card: {
    width: '70vw',
  },
  category: {
    marginBottom: 16,
    fontSize: 12,
    color: theme.palette.text.secondary,
  },
  title: {
    minHeight: 45,
  },
  author: {
    marginBottom: 12,
    color: theme.palette.text.secondary,
  },
});

export default styles;
